# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
# --------------------------------------------------------------------
# ^ above the line was added by `bundle init` and has not been touched
#
# GH Pages will not look at this file, 
# but it helps to have it for local development.

gem 'github-pages', group: :jekyll_plugins

# Whitelisted plugins not included in runtime dependencies.
gem "jekyll-octicons", :group => :jekyll_plugins
